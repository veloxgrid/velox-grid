fix: scroll sync issues in Phase 14 v0.8.0

Fixed horizontal and vertical scroll synchronization bugs:

**Horizontal Scroll (Header ↔ Body)**:
- Added bidirectional header scroll sync
- Header scroll now updates body/footer scrollLeft
- Body scroll updates header/footer scrollLeft

**Vertical Scroll (Fixed Right ↔ Body)**:
- Prevent infinite loop with source tracking
- Use isSyncing flag to prevent re-entry
- Skip updates when scrollTop values match
- Custom throttle to avoid TypeScript issues

Key improvements:
- Source parameter ('body' | 'fixedRight') tracks event origin
- Only update non-source elements to prevent circular updates
- Compare values before setting to avoid unnecessary updates

Bundle Size:
- UMD: 86.73 KB → 87.08 KB (+0.35 KB, gzip: 21.92 KB)
- ESM: 120.37 KB → 120.45 KB (+0.08 KB)

Files Modified:
- src/core/VeloxGrid.ts (attachEvents method)
- .claude/PROGRESS.md
