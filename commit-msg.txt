refactor(phase8): Extract rendering and event handling modules v0.8.0-step2-3

VeloxGrid.ts에서 렌더링 및 이벤트 핸들링 로직을 별도 모듈로 분리:

Step 2 - GridRenderer.ts (475줄):
- render(), renderHeader(), renderBody() - 전체 그리드 렌더링
- createHeaderCell(), createHeaderCheckbarCell() - 헤더 셀 생성
- createRowBase(), createCell(), createCheckbarCell() - Row & Cell 생성
- updateLoadingState(), updateRowValidationState() - 상태 업데이트
- 렌더링 로직을 단일 책임 원칙(SRP)에 따라 분리

Step 3 - GridEventManager.ts (425줄):
- attachEvents(), detachEvents() - 이벤트 리스너 등록/해제
- handleRowClick(), handleCellClick(), handleRowDoubleClick() - 클릭 이벤트
- handleKeyDown(), handleArrowKey(), handleEnterKey(), handleTabKey() - 키보드 이벤트
- handleResize(), handleScroll() - 브라우저 이벤트
- scrollCellIntoView(), scrollRowIntoView() - 스크롤 유틸리티

Benefits:
- VeloxGrid.ts 크기 감소 예정 (2,776줄 → 목표 ~1,900줄)
- 렌더링과 이벤트 로직의 명확한 분리
- 테스트 및 유지보수 용이성 향상
- Facade 패턴 유지 (VeloxGrid는 Public API만 유지)

Bundle Size (unchanged):
- UMD: 69.01 KB (gzip: 17.62 KB)
- ESM: 93.79 KB (gzip: 21.12 KB)
- CSS: 15.38 KB (gzip: 3.06 KB)

Files Added:
- src/core/GridRenderer.ts (475줄)
- src/core/GridEventManager.ts (425줄)

Files Modified:
- src/core/index.ts (모듈 export 추가)
- .claude/PROGRESS.md (Step 2-3 완료 표시)

Architecture:
```
VeloxGrid (Facade)
├── GridRenderer (렌더링 담당)
├── GridEventManager (이벤트 핸들링)
├── GridHistory (Undo/Redo)
├── GridSelection (선택 관리)
├── GridEditor (편집 관리)
└── ... (기타 모듈)
```

Next: Step 4-6 - Filter, Menu, Drag 모듈 분리
