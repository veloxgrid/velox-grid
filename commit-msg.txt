feat: customizable context menu and outside click close

1. Context Menu Customization:
   - Add ContextMenuItem, ContextMenuContext, ContextMenuOptions types
   - Support custom headerItems in contextMenu options
   - Support showDefaultItems option (true by default)
   - Support disabled and visible callbacks per item
   - Support shortcut display in menu items
   - Support custom className for menu items

2. Outside Click Menu Close:
   - Fix handleOutsideClick to close both filter popup and column menu
   - Add proper listener cleanup in closeColumnMenu()
   - Avoid removing listener when other popups still open

Usage example:
```typescript
const grid = new VeloxGrid(container, {
  columns: [...],
  contextMenu: {
    showDefaultItems: true,
    headerItems: [
      { id: 'custom', label: 'Custom Action', icon: '⚡', action: (ctx) => {...} },
      { type: 'separator' },
      { 
        id: 'conditional', 
        label: 'Conditional Item',
        disabled: (ctx) => ctx.selectedRows.length === 0,
        action: (ctx) => {...}
      }
    ]
  }
});
```
